{
  "name": "queue_rate-limiter",
  "version": "1.0.0",
  "description": "This project implements a Node.js API cluster with two replica sets, featuring a task queuing system with rate limiting. It processes user tasks at a rate of 1 task per second and 20 tasks per minute for each user ID.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cluster": "^0.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "express-slow-down": "^2.0.3",
    "ioredis": "^5.4.1"
  }
}
